This is a project where we will creat a small program
That handles lits system pets and owners

Initial features to work with:

### Feature: Create JSON

This feature can be completded
*create a folder json and two files in the folder:
* pets.json - an array of 3 different objects where each object has the properties name and species for example:

´´´json
{
    "id": 1;
    "name": "Garfield";
    "species": "Cat"
}


´´´json
{
    "id": 1;
    "mname": "Jane Doe";
    "email": "Jane@doe.com"

}
´´´´

### feature: add  more info

